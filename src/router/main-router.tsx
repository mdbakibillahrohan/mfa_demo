// import { Outlet } from "react-router";
import { type RouteObject } from "react-router";

const MainRouter: RouteObject[] = [
  {
    path: "",
    element: <>I am outlet of a module</>,
    // children: attendanceRouter,
  },
];
export default MainRouter;
